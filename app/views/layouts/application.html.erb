<!DOCTYPE html>
<html>
  <head>
    <title>Worldflavors</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%# Installation de MapBox %>
    <%# <script src='https://api.mapbox.com/mapbox-gl-js/v3.2.0/mapbox-gl.js'></script> %>
    <link href='https://api.mapbox.com/mapbox-gl-js/v3.2.0/mapbox-gl.css' rel='stylesheet' />

  </head>

  <body>




    <%= render "shared/flashes" %>


    <div class="<%= "body-margin" unless current_page?(root_path) || current_page?(new_user_registration_path) || current_page?(new_user_password_path) %>">
      <%= yield %>
    </div>

    <%= render "shared/new_navbar" unless current_page?(root_path) || current_page?(new_user_registration_path) || current_page?(new_user_password_path) %>

  </body>
</html>
